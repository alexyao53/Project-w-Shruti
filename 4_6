def computepay(hours, rate):

    try:
        if hours > 40:
            rate*=1.5
        pay = rate*hours
        print("Pay: " + str(pay))
    except:
        print("Please enter a number")

computepay(45, 10)